import { RequestPayload } from '../types/global';
import { initAction } from './navigationActions';
declare const _default: (state: RequestPayload | undefined, action: initAction) => RequestPayload;
export default _default;
